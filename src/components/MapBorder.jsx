import React from 'react'
import range from 'lodash/range'
import classNames from 'classnames'

import styles from '../styles/TopBorder.module.scss'

export default function({count, isVertical=false}) {
  return (
)
}